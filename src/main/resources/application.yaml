spring:
  main:
    web-application-type: none
    banner-mode: off
  application:
    name: mcp_server
  ai:
    mcp:
      server:
        name: mon_serveur_mcp
        version: 0.0.1
        stdio: true
